# Copy this file to backend/.env (same folder as package.json). The server loads backend/.env explicitly.
# Optional: when MYSQL_HOST is set, citizen profiles are stored in MySQL instead of data/profiles.json
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=risk_radar

PORT=5000
