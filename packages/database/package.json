{
  "name": "@risk-radar/database",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "db:push": "node scripts/apply-schema.mjs",
    "db:migrate": "echo \"Apply SQL migrations with psql or your tool (see packages/database/schema.sql)\" && exit 0"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "typescript": "^5.5.3"
  }
}
